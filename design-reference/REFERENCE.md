# Approved Studio Dark reference

Ahmed Hossam approved this visual direction. These are the exact public files from the approved design snapshot c416bfae87ebfec8ec282961b23c805984cd46fd.

Use them as the visual/content reference for the new portfolio, following docs/portfolio/design-spec.md. The original reference is buildless HTML/CSS/JS; the new implementation plan uses a static Astro shell and separate React/Vite demos.

The Preview demo here only explains a proposed scenario. It is not an original-UI application demo. No browser QA or runtime performance results have been claimed.

Hosting identity, Git metadata, credentials, and client application source are deliberately excluded. Do not import the original Site's hosting settings into a new repository. The included CV is the current user-supplied PDF, unchanged.
