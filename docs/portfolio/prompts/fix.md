# Repair a specific defect

We are fixing a defect in the existing Ahmed Hossam portfolio. Read `AGENTS.md`, `STATE.md`, the relevant design/demo spec, and the code involved.

Problem to investigate:

[Replace this line with the page/demo, steps to reproduce, actual behavior, expected behavior, and any screenshot/error you have. Omit details you do not know.]

Reproduce or narrow the actual issue before changing code. Make the smallest coherent fix that preserves the approved design and the current phase's architecture. Do not redesign unrelated sections, add features, upgrade unrelated dependencies, or connect to a client service as a workaround.

If the defect is visual or interactive, this request includes the relevant browser check where tooling is available. If it affects local business state, verify the meaningful before/after quantities, costs, or transitions and reset behavior. Run only the tests needed to resolve the defect and relevant regression risk.

Update the applicable validation report and `STATE.md`. Report the cause, visible correction, checks performed, and any remaining limitation. Work locally unless the current session separately requests publishing this repair to a specified target.
