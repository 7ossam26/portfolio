# Demo contract boundary

This package owns the framework-free demo IDs, lifecycle message envelope, runtime validator, and build-time registry data shared by the portfolio host and future child demos.

Registry availability is deliberately false until the corresponding original-UI scenario passes its own phase gate. The internal host harness is test infrastructure and is not part of this production registry.
